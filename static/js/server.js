function wait_msg() {
   alert("It may take little more time to execute");
}

function scroll_to() {
   var element_to_scroll_to = document.getElementById('try');
   element_to_scroll_to.scrollIntoView();
}

